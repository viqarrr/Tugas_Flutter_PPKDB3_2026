import 'package:flutter/material.dart';

class CenterDay6 extends StatelessWidget {
  const CenterDay6({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("WAUW INI DI TENGAH")),
    );
  }
}